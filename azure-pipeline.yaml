


resources:
  repositories:
  - repository: MyGitHubRepo
    type: github
    endpoint: Mygit
    name: balaji-9702/Templates
  
trigger: none

variables:
- template: values.yaml

jobs:
- job: Build
  condition: and(succeeded(), eq('${{ variables.BuildtechBackend }}', 'Net'))
  pool:
    vmImage: ${{ variables.vmImage }}
  steps: 
  - checkout: MygithubRepo
  - template: .Net-CI.yaml
  - template: .Net-CD.yaml
  

- job: PythonDeploy
  condition: and(succeeded(), eq('${{ variables.BuildtechBackend }}', 'python'))
  pool:
    vmImage: ${{ variables.vmImage }}
  steps:
  - checkout: MygithubRepo
  - script: echo "hi"
  
  

  
  
  
  

  

