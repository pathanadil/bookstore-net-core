


resources:
  repositories:
  - repository: MyGitHubRepo
    type: github
    endpoint: Mygit
    name: balaji-9702/Templates
    ref: Conditioncheck

    
  
trigger: none

variables:
- template: values.yaml

jobs:
- job: Build
  condition:  eq('${{ variables.BuildtechBackend }}', 'Net')
  pool:
    vmImage: ${{ variables.vmImage }}
  steps: 
  - ${{ if eq('${{ variables.Build }}', 'true') }}:
  - template: Restore.yaml@MyGitHubRepo
  - ${{ if eq('${{ variables.Build }}', 'true') }}:
  - template: Build.yaml@MyGitHubRepo
  

- job: PythonDeploy
  condition: eq('${{ variables.BuildtechBackend }}', 'python')
  pool:
    vmImage: ${{ variables.vmImage }}
  steps:
  - checkout: MyGitHubRepo
  - script: cd D:\a\1\s
  - script: ls
  - template: python-CI.yaml@MyGitHubRepo
  - script: echo "hi"
  
  

  
  
  
  

  

